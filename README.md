# WrapPointer-Analyzer
    Log Example) // A, B, C, D -> address?
    new = { A = { new = { B } } }
    new = { C = { new = { D } } }
    assign = { B = C }
    assign = { D = A }
    delete = { A }
    delete = { C }
    
# Log -> My Group Manager -> Analysis and Visualization.
    
